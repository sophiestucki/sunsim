from setuptools import setup, find_packages

setup(
    name="starsim",
    version="0.1",
    packages=find_packages(),  # automatically finds 'starsim' folder
    install_requires=[
        # add dependencies here if any, e.g., "numpy>=1.24"
    ],
)
